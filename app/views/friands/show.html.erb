

<div class="card" style="width: 30rem;">
  <div class="card-body">
    <h5 class="card-title"> <%= @friand.first_name %> <%= @friand.last_name %></h5>
    <h6 class="card-subtitle mb-2 text-muted"><%= @friand.email %></h6>
    <h6 class="card-subtitle mb-2 text-muted"> <%= @friand.phone %></h6>
    <h6 class="card-subtitle mb-2 text-muted"><%= @friand.facebook %></h6>
  
  </div>
</div>
</p>
<%= link_to 'Edit', edit_friand_path(@friand), class:"btn btn-warning" %> 
<%= link_to 'Back', friands_path, class:"btn btn-primary" %>

